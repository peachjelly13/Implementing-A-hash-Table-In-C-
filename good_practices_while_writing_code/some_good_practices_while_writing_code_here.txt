1.) The use of static:
It ensures that ht_new_item is encapsulated 
and accessible only within the same source file, which is a common practice to maintain 
modularity and prevent accidental misuse of internal functions.